# Godot 4 imported/cache data
.godot/

# Godot export output
export/
*.exe
*.pck

# Local Claude Code settings (personal, not shared)
.claude/settings.json
.claude/settings.local.json

# Windows ReplaceFile leftovers (in-use file swaps, e.g. GDExtension DLLs)
~*.TMP
~*.dll

# Build output from tools/build_manuals.ps1 — the two chapter catalogs are the
# source of truth; a committed PDF would be a third in-tree copy of every figure.
build/
